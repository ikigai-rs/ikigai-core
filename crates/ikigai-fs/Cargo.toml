[package]
name = "ikigai-fs"
description = "Capability-confined file endpoint (default-deny, jailed to a root)."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
async-trait = "0.1"
ikigai-core = { path = "../ikigai-core" }
ikigai-vocab = { path = "../ikigai-vocab" }

[dev-dependencies]
futures = "0.3"
